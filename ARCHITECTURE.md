# Microverse — architecture notes

A living record of decisions made so far. This is not a spec — it's a snapshot to stop us re-deciding the same thing twice.

## Philosophy

Microverse is a polyglot playground: every component gets to have its own technology, its own personality, and its own reason for existing. Consistency isn't the goal — clarity of *role* is.

## The four tiers

```
Applications        — the front doors (TaskFusion, Admin, LAReports)
Domain services      — one specialist trick each, customer-facing brand names
Business services    — the narrator, owns the plot
Platform services     — plumbing, zero business opinions, fully generic
Infrastructure        — the ground everything stands on
```

**The test for each tier**, in case a new service shows up later and you're not sure where it goes:
- **Platform service** — could this get dropped into a totally unrelated app unchanged? If yes, it's plumbing.
- **Domain service** — does it do exactly one specialist trick, with a real opinion about *what* it's analyzing/processing? If yes, it's a specialist.
- **Business service** — does it know what an Order or a Quest *is*? If yes, it's the narrator.

## Domain services (customer-facing)

| Service | Tech | What it does | Status |
|---|---|---|---|
| GoFeeler | Go | Sentiment analysis on uploaded chats/emails/comments | Online, but naive keyword-matching — needs a real lexicon or LLM-based upgrade |
| SpringPix | Java/Spring | Image and GIS processing — does the raster hotspot analysis, backed by PostGIS | Basic app |
| PyReel | Python | Video processing | Basic app |
| NetCruncher | .NET | Calculation engine | Exists |
| Djaboard | Django (renamed from DjaPorts) | Reporting + kudos leaderboard — computes tiers/badges as JSON, React just renders it | Building |
| elixtempo | Elixir | Time tracking per analyst per Quest (OTP concurrency — many cheap live sessions) | Designing |
| rustledger | Rust | Invoices/billing ledger, consumes elixtempo's time-entry events off Kafka | Designing |
| RubyKudos | Ruby | Raw kudos event capture | Not started |

## Business services (the narrator)

| Service | Tech | Role |
|---|---|---|
| order-service | Go | Customer creates an Order, uploads media |
| task-service | Go | Owns the shared task pool (see below); PM assigns Tasks to analysts |
| workflow | Java (Camunda/Zeebe) | Orchestrates the full Order → Task → time → bill → kudos sequence as an explicit state machine |

## Platform services (plumbing)

| Service | Tech |
|---|---|
| api-gateway | Kong/Traefik (off-the-shelf, no custom code) |
| storage | Go — thin MinIO/S3 wrapper |
| asset-service | Rust — versioning + permissions on top of `storage` |
| event-bus | Go (Watermill) — shared abstraction over Kafka + RabbitMQ (renamed from "messaging" to free that name up for the human-facing feature below) |
| messaging | Human (and possibly agent) communication feature — popup notification, newsfeed entry, email; no dedicated chat, async only |
| notification-service | Node.js/TypeScript — decides who needs to know, hands off to email-service; handles delivery mechanics underneath `messaging` |
| email-service | Node.js/TypeScript (nodemailer → MailHog) |
| search-service | Python (Elasticsearch) — permission-specific search over a user's own work; human-only, not for agents |
| audit | Java |
| scheduler | Java (Quartz) |
| billing-service | Python (Stripe SDK) — payment collection, tells rustledger "payment received" |
| tracking-service | .NET — general product usage analytics (not time tracking — that's elixtempo) |

## Kafka vs RabbitMQ

- **Kafka = the scroll.** Immutable, replayable. Anything that needs a permanent record: `time_entry.completed`, `sentiment.analyzed`, `kudos.given`, tier changes.
- **RabbitMQ = the pigeon.** Fire-and-forget delivery: "send this email," "process this image," "nudge this analyst." Disposable — if nobody's home, it just doesn't land.
- Most interesting events do both: Kafka remembers it happened, RabbitMQ tells someone right now.

## Entity model

- **Customer** — a company or an individual human. 1:n — a company can have multiple customer users under it. Creates Orders, has Invoices.
- **Order** — what a customer submits (media + service type). Carries a `service` tag (e.g. `gofeeler`) tying it to the domain service it came through. Extra detail (comments, etc.) lives as attributes on the Order/Task rather than spawning separate records.
- **Task** — an Order once a PM assigns it to an Analyst. Stays 1:1 with its Order.
- **Analyst** — human or agentic. Has a rating, performance history, message history to their PM.
- **Project Manager** — human or agentic. Assigned to a specific company, handles that company's Orders. Claims Tasks from the pool, assigns to Analysts, gives kudos.
- **Invoice** — generated by rustledger from elixtempo's tracked time.
- **Kudos** — given by PM or Customer to an Analyst; feeds Djaboard's leaderboard.

**ID convention:** all entity IDs are UUIDs, not incremental integers — prevents enumeration (guessing adjacent order/task IDs), lets services generate IDs independently without coordination, and keeps object storage paths from leaking business volume. Use time-ordered UUIDs (UUIDv7, or `gen_random_uuid()` via pgcrypto where v7 isn't available) rather than random UUIDv4, to avoid B-tree index fragmentation on insert-heavy tables like the task pool.

## Roles and permissions

Two-dimensional model, requiring both to grant access:

- **Platform function** — `platform:customer`, `platform:project-manager`, `platform:analyst`, `platform:reviewer`
- **Service scope** — `service:gofeeler`, `service:springpix`, `service:pyreel`, etc.

A person needs both claims for a given action — e.g. GoFeeler's Create Order page requires `platform:customer` + `service:gofeeler`. This replaces the earlier single combined-string model (`gofeeler:project-manager`) — someone who works across multiple services now holds one platform function role plus each relevant service scope, instead of a separate combined role per service.

- Humans can hold multiple service scopes at once.
- Agents are simply assigned roles the same way — no separate agent-identity model needed.
- Roles map directly to Keycloak realm/client roles — permission checks are just "does the JWT contain both claims," not custom application logic.

**Role vs. assignment — an important distinction.** A role only grants broad *eligibility* ("can this person ever act as an analyst for GoFeeler") — it is never per-project. The actual per-task function comes from `Task.assignee_id`. A person being analyst on one Order and reviewer on another doesn't need a third role dimension — they simply hold both `platform:analyst` and `platform:reviewer` (plus `service:gofeeler`), and which one applies on any given Task is just whatever they're currently assigned as. Conflict-of-interest rules (e.g. blocking someone from reviewing their own prior work, or work for a competing company) are **not** enforced by the role model — that's deferred as a future business decision, to be handled as an assignment-time check in task-service if/when it's actually needed.

## Task workflow

A Task has a Jira-ticket-style status, owned by `workflow` (Camunda):

```
unassigned → analyst → reviewer → done → paid → closed
                ↑___________|
                  (rejected)
```

- **Analyst** and **reviewer** are two distinct roles (`gofeeler:analyst`, `gofeeler:reviewer`) — one human can hold both, but a single Task always has exactly one active assignee at a time.
- **Backward transitions are allowed**: a reviewer can reject work, sending the Task back to `analyst`. Rejection requires an assignee — the transition isn't complete until a new analyst is picked, so a Task never sits unassigned mid-rejection.
- **Assignee vs owner** — these are different fields, deliberately, to avoid the Jira annoyance of a "done" ticket still sitting under someone's name:

  | State | Assignee | Owner |
  |---|---|---|
  | unassigned | — | — |
  | analyst | the analyst | the analyst |
  | reviewer | the reviewer | the reviewer |
  | done | — | PM |
  | paid | — | customer |
  | closed | — | — |

- Reaching **paid** unlocks the results for the customer to download.
- The assignee must always match the current state's required role — a transition shouldn't be allowed to complete without a valid assignee holding the right role for the new state.

## The task pool

- Lives in **task-service**, backed by **PostgreSQL** (not Mongo — this specifically needs `SELECT ... FOR UPDATE SKIP LOCKED` for safe concurrent claiming).
- Query shape:
  ```sql
  SELECT * FROM tasks
  WHERE status = 'unassigned'
    AND service = ANY(:user_roles)
  ORDER BY created_at
  FOR UPDATE SKIP LOCKED
  LIMIT 1;
  ```
- No separate pooling service or permission layer needed — the role filter is baked into the query itself.

## Dashboard / UI notes

- Domain-service cards use `heroImage` (or a tinted icon block), `name`, `description`; `onOpen` does a full page navigation to the service's own subdomain — the dashboard is a launcher, not an iframe host.
- Plumbing (platform-services) gets an internal ops view, badged by language-homage color + generic functional icon (not customers-facing).
- Admin dashboard needs a status filter (All / Online / In progress) rather than always showing raw build status.
- Orders/Tasks landing page: status, deadline, assignee (linked), green/yellow/red urgency dot, link to customer. Unassigned gets a neutral gray dot, not forced into the urgency scale.
- Theme: sampled from a starfield/network background image — dark mode stays close to the source (deep navy `#05061A`/`#0B0F2E`, cyan accent `#4DD8FF`), light mode inverts role rather than literal color (deep navy becomes the accent/text color, cyan lightens to `#0EA5D9` for contrast on white).

## GoFeeler end-to-end flow (first fully designed service flow)

1. **Customer** creates an Order on GoFeeler's Create Order page (`platform:customer` + `service:gofeeler`) — uploads content via a field that stores to MinIO.
2. **PM** (`platform:project-manager` + `service:gofeeler`) gets a notification (bell icon, unread count, popup on click); clicking it loads the order page.
3. The **recommendation agent** populates a word-cloud element with candidate analysts (human or agentic), sized/weighted by fit.
4. PM selects an analyst → order becomes an assigned Task.
5. **Analyst** sees the notification, opens the task, clicks "Analyse." The task page includes a sentiments multiselect element. GoFeeler returns sentiment scores (LLM integration being considered for this step).
6. Analyst reviews the text, can add versioned notes/comments, adds recommendations if results are sufficient, then moves the task to review — **default reviewer is the PM** (can be reassigned to a dedicated `service:gofeeler` + `platform:reviewer` holder).
7. PM approves → bill created for the customer → customer pays *(payment flow itself is a later roadmap item)*.

**UI pattern:** steps 2–7 all happen on one unified Order/Task detail page, not separate forms per step — the available actions render based on `(viewer's platform role, task's current state)`. A PM sees the analyst picker only when status is unassigned; an analyst sees "Analyse" and notes only while assigned to them; a reviewer sees approve/reject only in the reviewer state; a customer gets a read-only view with download unlocked once paid. The list, this detail view, and the Create Order form live on one screen as a master-detail split view — clicking a row shrinks the list and slides in the relevant panel, filling the reclaimed width rather than staying narrow. Below a mobile breakpoint this falls back to stacked single-panel navigation instead of a cramped split (see ROADMAP.md Proposals for the layout details and a CSS gotcha worth knowing before implementing it).

**Build sequence:** see `ROADMAP.md`'s GoFeeler section for the five-branch dev plan (form scaffold → dynamic detail view with dummy data → real form functionality → task detail functionality → LLM integration).

## Open questions

- Is a PM assigned per-Order, or one shared pool of PMs within a company? *(Partially resolved — a PM is assigned to a company and handles that company's orders; still open whether it's one dedicated PM per company or a pool.)*
- On rejection, does a Task return to the *same* analyst, or back into the pool for anyone with that role?
- `intelligence/` folder taxonomy (agents, ai-tools, knowledge, mcp, memory, models, prompts, workflows) — which of these are actual runnable services vs. just config/artifacts other services consume. Still a work in progress.
- Agent task-recommendation feature (recommend tasks to an analyst based on performance + cost) — todo, not yet designed.
- Customers page and Analysts page — basic light/dark versions built (profile header, orders/invoices list, metric cards + current tasks); still missing graphs and message-history sections.

## Recently resolved

- **Agents and search-service** — human-only, not for agents.
- **`messaging` naming collision** — renamed the pub/sub abstraction to `event-bus`; `messaging` is now the human (and possibly agent) communication feature (popup, newsfeed, email — no chat).
- **Order-to-Task cardinality** — confirmed 1:1; extra detail lives as attributes, not separate Tasks.
- **Customer entity** — can be a company or an individual; 1:n, a company can have multiple customer users.
- **PM-to-company assignment** — a PM is assigned to a specific company and works that company's orders.
- **Rejection assignee** — rejection requires picking a new assignee immediately; a Task never sits unassigned mid-rejection.
