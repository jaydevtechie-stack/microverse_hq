const reportWebVitals = (metric) => {
  console.log(metric); // Log the metric data in the console, you can replace this with sending the data to an analytics endpoint.
};

export default reportWebVitals;
