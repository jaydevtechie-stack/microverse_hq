# billing-service

**Status:** not yet implemented — folder scaffold only.

Middleware in front of
[`rustledger`](../../domain-services/rustledger) (the Rust billing
service) — the layer other platform/domain services talk to instead of
calling RustLedger's line-item/invoice API directly. Exact
responsibilities still need designing.
