{
  "name": "email-service",
  "version": "1.0.0",
  "description": "Shared email-sending service — renders templates and sends via MailHog in dev",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^9.0.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
